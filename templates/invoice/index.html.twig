{% extends 'base.html.twig' %}

{% block title %}Invoice index{% endblock %}

{% block body %}
    <h1>Invoice index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>InvoiceNumber</th>
                <th>IssueDate_at</th>
                <th>DueDate_at</th>
                <th>TotalAmount</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for invoice in invoices %}
            <tr>
                <td>{{ invoice.id }}</td>
                <td>{{ invoice.invoiceNumber }}</td>
                <td>{{ invoice.issueDateAt ? invoice.issueDateAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ invoice.dueDateAt ? invoice.dueDateAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ invoice.totalAmount }}</td>
                <td>{{ invoice.status }}</td>
                <td>
                    <a href="{{ path('app_invoice_show', {'id': invoice.id}) }}">show</a>
                    <a href="{{ path('app_invoice_edit', {'id': invoice.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_invoice_new') }}">Create new</a>
{% endblock %}
